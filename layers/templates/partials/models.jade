div.modal.fade(role="dialog", id="editProjectTitle", tabindex="-1", aria-labelledby="changeName", aria-hidden="true")
	div.modal.dialog
		div.modal.content
			div.modal-header
				button(type="button", class="close", data-dismiss="modal", aria-label="Close")
					span(aria-hidden="true")&times;
				h4.modal-title(id="changeName") Change the title of your project
			div.modal-body
				div.form-group
					- csrf_token
					input.form-control(type="text", id="titleInput", name="New_Title")
					button(type="submit").btn.btn-primary Change
			div.modal-footer
				button(type="button", class="btn btn-default, data-dismiss="modal") Close
				button(type="button", class="btn btn-primary") Save Changes


div.modal.fade(role="dialog", id="editProjectDescription", tabindex="-1", aria-labelledby="changeDescription", aria-hidden="true")
	div.modal.dialog
		div.modal.content
			div.modal-header
				button(type="button", class="close", data-dismiss="modal", aria-label="Close")
					span(aria-hidden="true")&times;
				h4.modal-title(id="changeName") Change your project's description
			div.modal-body
				div.form-group
					- csrf_token
					input.form-control(type="text", id="titleInput", name="New_Title")
					button(type="submit").btn.btn-primary Change
			div.modal-footer
				button(type="button", class="btn btn-default, data-dismiss="modal") Close
				button(type="button", class="btn btn-primary") Save Changes
